# Тестовые скрипты для калибровки переводчика

Эта директория содержит скрипты для тестирования и калибровки компонентов переводчика **без использования audio streaming**. Это позволяет отладить каждый компонент отдельно перед запуском полного pipeline.

## 📁 Структура

```
tests/
├── test_translation.py    # Тестирование Translation (OpenRouter LLM)
├── test_tts.py            # Тестирование TTS (XTTS-v2)
├── test_pipeline.py       # Полный pipeline (Translation + TTS)
└── README.md              # Эта инструкция
```

## 🎯 Назначение

### 1. `test_translation.py` - Калибровка перевода

**Что тестирует:**
- Качество перевода с английского на русский
- Сохранение контекста между предложениями
- Скорость работы OpenRouter API
- Корректность использования модели Claude 3.5 Sonnet

**Как запустить:**
```bash
python tests/test_translation.py
```

**Что смотреть:**
- Точность перевода каждого предложения
- Сохраняется ли смысл и контекст
- Время отклика API (latency)

**Параметры для калибровки** (в `config.yaml`):
- `llm.model` - выбор модели (по умолчанию: `anthropic/claude-3.5-sonnet`)
- `llm.temperature` - креативность перевода (0.0-1.0)
- `llm.max_tokens` - максимальная длина ответа

---

### 2. `test_tts.py` - Калибровка озвучки

**Что тестирует:**
- Качество синтеза речи XTTS-v2
- Произношение русских слов
- Скорость генерации аудио
- Использование VRAM GPU

**Как запустить:**
```bash
python tests/test_tts.py
```

**Что смотреть:**
- Прослушайте файлы в `test_output/`
- Оцените естественность голоса
- Проверьте правильность произношения
- Измерьте latency (должно быть ~2-3 сек)

**Параметры для калибровки** (в `config.yaml`):
- `tts.speed` - скорость речи (0.5-2.0)
- `tts.temperature` - вариативность голоса (0.0-1.0)
- `tts.voice_sample_path` - путь к образцу голоса

---

### 3. `test_pipeline.py` - Полный pipeline

**Что тестирует:**
- Связку Translation → TTS
- Общую latency всего процесса
- Сохранение контекста через весь pipeline

**Как запустить:**
```bash
python tests/test_pipeline.py
```

**Что смотреть:**
- Общая задержка (должна быть ~3.5-5.5 сек)
- Соответствие перевода и озвучки
- Нет ли потери качества на стыках

**Выходные данные:**
- Аудио файлы: `test_output/pipeline_XX_YYYYMMDD_HHMMSS.wav`
- Статистика latency в консоли

---

## 📊 Тестовые данные

### `test_data/sample_text.txt`

Содержит **20 предложений на английском** про AI/ML тематику:
- Разнообразная лексика
- Технические термины
- Разная длина предложений
- Проверка контекста между предложениями

Вы можете заменить этот файл на свой текст для других тематик.

---

## 🔧 Процесс калибровки (рекомендуемый порядок)

### Шаг 1: Проверка Translation

```bash
python tests/test_translation.py
```

**Что оцениваем:**
1. Точность перевода специальных терминов
2. Сохранение смысла и tone of voice
3. Использование контекста предыдущих предложений

**Настройка:**
- Если переводы слишком "креативные" → уменьшите `llm.temperature`
- Если слишком буквальные → увеличьте `llm.temperature`
- Если обрезаются длинные предложения → увеличьте `llm.max_tokens`

---

### Шаг 2: Проверка TTS

```bash
python tests/test_tts.py
```

**Что оцениваем:**
1. Естественность голоса
2. Правильность произношения
3. Скорость речи
4. Интонация

**Настройка:**
- Если речь слишком быстрая → уменьшите `tts.speed` (например, 0.9)
- Если слишком монотонная → увеличьте `tts.temperature`
- Если голос не нравится → замените `voice_sample_path`

---

### Шаг 3: Полный pipeline

```bash
python tests/test_pipeline.py
```

**Что оцениваем:**
1. Общая latency (цель: < 5 секунд)
2. Качество на выходе
3. Соответствие перевода и озвучки

**Оптимизация:**
- Если latency слишком большая:
  - Translation: попробуйте более быструю модель
  - TTS: проверьте использование GPU
- Следите за VRAM usage (~3GB нормально)

---

## 📈 Метрики производительности

### Целевые показатели:

| Компонент    | Target Latency | Acceptable Range |
|-------------|----------------|------------------|
| Translation | 1.0-2.0s       | 0.5-3.0s         |
| TTS         | 2.0-3.0s       | 1.5-4.0s         |
| **Total**   | **3.5-5.5s**   | **3.0-7.0s**     |

### VRAM Usage:
- XTTS-v2: ~3GB
- Общее: ~3.5GB (комфортно для 16GB GPU)

---

## 🎛️ Основные параметры в `config.yaml`

### Translation (LLM):
```yaml
llm:
  model: "anthropic/claude-3.5-sonnet"  # Модель
  temperature: 0.3                       # Креативность (0.0-1.0)
  max_tokens: 500                        # Макс. длина
```

### TTS (XTTS-v2):
```yaml
tts:
  voice_sample_path: "voice_samples/russian_voice.wav"
  language: "ru"
  speed: 1.0          # Скорость речи (0.5-2.0)
  temperature: 0.7    # Вариативность (0.0-1.0)
```

---

## 🚀 После калибровки

Когда вы довольны результатами тестов:

1. ✅ Translation работает корректно
2. ✅ TTS озвучивает естественно
3. ✅ Latency в пределах нормы

**Можно запускать полный сервер:**
```bash
python app/main.py
```

Откройте http://localhost:8000 и тестируйте с реальным audio streaming!

---

## 📝 Примечания

- Все тесты работают **асинхронно** (async/await)
- Аудио файлы сохраняются в `test_output/`
- Логи выводятся в консоль
- Можно запускать скрипты многократно для A/B тестирования параметров

---

## ❓ Troubleshooting

### Ошибка: "API key not found"
- Проверьте файл `.env`
- Убедитесь, что там есть `OPENROUTER_API_KEY` и `GROQ_API_KEY`

### Ошибка: "CUDA out of memory"
- TTS требует GPU с минимум 4GB VRAM
- Закройте другие GPU-приложения

### Перевод некорректный
- Попробуйте другую модель в `config.yaml`
- Измените `temperature` для Translation

### Голос звучит неестественно
- Замените файл `voice_samples/russian_voice.wav` на другой образец
- Убедитесь, что образец качественный (чистый звук, нет шумов)

---

**Удачной калибровки! 🎯**
